# Competition images — NOT redistributable (DAC FindIT 2026 rules)
data/
dataset/
images/
*.jpg
*.jpeg
*.png
# ...but DO keep the curated preview/result images in docs/
!docs/*.jpg
!docs/*.jpeg
!docs/*.png

# Trained fold weights (~108 MB each) — not redistributable, too large
model/
*.pth
*.pt
*.ckpt

# Jupyter / Python
.ipynb_checkpoints/
__pycache__/
*.pyc
.venv/
venv/

# Environment / secrets
.env
.env.*
